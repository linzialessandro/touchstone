# Longest common subsequence length

Implement:

```python
def lcs_length(a: str, b: str) -> int:
    ...
```

## Spec

Return the length of the longest common **subsequence** (not substring).
Empty string → `0`.

## Example

`lcs_length("abcde", "ace")` → `3`
