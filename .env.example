# Pick one baseline provider for v0.1 (Grok or Gemini).
# Never commit real keys.

# --- Grok (xAI) ---
# XAI_API_KEY=
# CODE_REASON_PROVIDER=grok
# CODE_REASON_MODEL=grok-4.5

# --- Gemini (Google) ---
# GEMINI_API_KEY=
# CODE_REASON_PROVIDER=gemini
# CODE_REASON_MODEL=gemini-3.6-flash

# Optional shared overrides
# CODE_REASON_PROVIDER=mock
# CODE_REASON_MODEL=mock-echo
